import { makeStyles } from "@material-ui/styles"

const styles = makeStyles({
    list: {
        width: 250
    },
    link: {
        textAlign: 'center'
    },
    linkText: {
        fontSize: 24
    },
})

export default styles