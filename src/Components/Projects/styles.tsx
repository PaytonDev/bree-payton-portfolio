import { makeStyles } from "@material-ui/styles";

const styles = makeStyles({
  projectHeader: {
    fontSize: 48,
    fontWeight: 700,
    marginTop: "5vh",
    marginBottom: "5vh",
  },
  projectSection: {
    maxWidth: "100vw",
    minHeight: "90vh",
    marginLeft: 30,
    marginRight: 30,
  },
});

export default styles;
