import { makeStyles } from "@material-ui/styles";

const styles = makeStyles({
    projectHeader: {
        fontSize: 48,
        fontWeight: 700,
        marginTop: "10vh",
        marginBottom: "10vh",
    },
    projectSection: {
        maxWidth: "100vw",
        minHeight: "100vh",
        marginLeft: 30,
        marginRight: 30,

    },
})

export default styles